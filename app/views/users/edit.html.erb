<h2>Edit</h2>

<% if @user == current_user %>
	<%= flash[:error] %>
	<%= render 'shared/edit_user_info' %>
	<%= render 'shared/edit_user_picture' %>
<% else %>
	<p>You aren't allowed to edit this page.</p>
<% end %>